server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    config: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations